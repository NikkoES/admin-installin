<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fadil.admininstallin">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_main_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_main_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashScreenActivity" android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.TambahKurirActivity" />
        <activity android:name=".activity.DetailPemesananActivity" />
        <activity android:name=".activity.InformasiOrderActivity" />
        <activity android:name=".activity.DashboardActivity"></activity>
    </application>

</manifest>